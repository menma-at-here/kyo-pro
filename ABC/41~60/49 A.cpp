#include <bits/stdc++.h>
using namespace std;
 
int main() {
  char alpha;
  cin >> alpha;
  if (alpha == a || alpha == i || alpha == u || alpha == e || alpha == o){
    cout << "vowel" << endl;
  }
  else{
    cout << "
